import "./App.css";
import { Routes, Link, Navigate, Route, useNavigate } from "react-router-dom";
import { makeAutoObservable } from "mobx";
import { observer } from "mobx-react";

function App() {
  return (
    <div className="App">
      <header className="App-header"></header>
    </div>
  );
}

export default App;
